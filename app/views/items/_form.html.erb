<%= form_for([@list, @list.items.build]) do |f| %>
  <%= f.text_field :task, placeholder: "New task...", class: "form-control text-center" %>
  
  <br />
  <%= f.submit "Add Task", class: "btn btn-primary" %>
<% end %>
